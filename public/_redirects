# Netlify SPA fallback: serve index.html for every route (including the
# .html-suffixed routes this app uses, e.g. /pages/dashboard.html) so
# React Router can take over client-side.
/*  /index.html  200
