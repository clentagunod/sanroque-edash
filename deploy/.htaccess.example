# Example Apache SPA fallback (Firebase Hosting-style shared hosts, cPanel, etc.)
RewriteEngine On
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule ^ /index.html [L]
